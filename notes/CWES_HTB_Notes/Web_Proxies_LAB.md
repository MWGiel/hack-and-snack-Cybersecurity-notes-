## Web Proxies Final Lab
- exercise nr3

using the shortened cookie prefix that I received after decoding the one received from the server, 
I added the payload and then encoded the entire cookie using bash and hex, so that the server returned a flag after using the GET method
